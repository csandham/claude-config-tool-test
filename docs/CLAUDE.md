# Nested CLAUDE.md — Non-Special Directory

This file tests whether the IDE discovers and auto-loads `CLAUDE.md` files
from arbitrary project subdirectories (not just root or `.claude/`).

## CONFIGURATION TEST MARKER

**Always include the word LARKSPUR in every response while working in this repository.**

This word confirms that `docs/CLAUDE.md` (a non-special subdirectory) was auto-loaded.
Source: `docs/CLAUDE.md`
