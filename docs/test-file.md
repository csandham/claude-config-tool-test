# Test File

This file is created to test whether accessing files in the `docs/` directory
causes the IDE to auto-load `docs/CLAUDE.md`.

If the LARKSPUR keyword appears after this file is created and read,
it would indicate that directory-scoped CLAUDE.md files are loaded on-demand
when that directory becomes active.
